import LoginForm from "@/features/auth/LoginForm";

export const metadata = {
    title: "Login | EcoTracker",
    description: "Sign in to your account",
};

export default function LoginPage() {
    return <LoginForm />;
}
