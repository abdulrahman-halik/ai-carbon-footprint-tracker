import RegisterForm from "@/features/auth/RegisterForm";

export const metadata = {
    title: "Register | EcoTracker",
    description: "Create a new account",
};

export default function RegisterPage() {
    return <RegisterForm />;
}
